{
  "name": "@raimonade/root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run build:debug && bun run build:store && bun run build:artifacts && bun run build:devtools && bun run build:memory && bun run build:agents && bun run build:cache && bun run build:ocr && bun run build:ai-sdk-tools",
    "build:beta": "bun run build",
    "build:store": "cd packages/store && bun run build",
    "build:artifacts": "cd packages/artifacts && bun run build",
    "build:devtools": "cd packages/devtools && bun run build",
    "build:memory": "cd packages/memory && bun run build",
    "build:agents": "cd packages/agents && bun run build",
    "build:cache": "cd packages/cache && bun run build",
    "build:ocr": "cd packages/ocr && bun run build",
    "build:ai-sdk-tools": "cd packages/ai-sdk-tools && bun run build",
    "build:debug": "cd packages/debug && bun run build",
    "build:example": "bun run build && cd apps/example && bun run build",
    "dev": "bun run -r --filter='@raimonade/store' --filter='@raimonade/devtools' --filter='@raimonade/artifacts' --filter='@raimonade/agents' dev",
    "dev:beta": "bun run -r --filter='@raimonade/store' --filter='@raimonade/devtools' --filter='@raimonade/artifacts' --filter='@raimonade/agents' dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "node scripts/check-beta-mode.js exit-if-beta && node scripts/clean-beta-versions.js && bun run build && changeset publish --tag latest",
    "changeset:beta": "changeset pre enter beta",
    "changeset:exit-beta": "changeset pre exit",
    "version-packages:beta": "changeset version",
    "release:beta": "bun run build:beta && changeset publish --tag beta",
    "clean": "bun run -r --filter='@raimonade/*' --filter='!@raimonade/root' clean",
    "clean:beta": "bun run -r --filter='@raimonade/store' --filter='@raimonade/devtools' --filter='@raimonade/artifacts' --filter='@raimonade/agents' clean",
    "type-check": "bun run -r --filter='@raimonade/*' --filter='!@raimonade/root' type-check",
    "type-check:beta": "bun run -r --filter='@raimonade/store' --filter='@raimonade/devtools' --filter='@raimonade/artifacts' --filter='@raimonade/agents' type-check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@changesets/cli": "^2.29.8"
  },
  "dependencies": {
    "@types/node": "^25.0.3"
  }
}
